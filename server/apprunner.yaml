version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - apt-get update && apt-get install -y ffmpeg
run:
  command: npm start
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
